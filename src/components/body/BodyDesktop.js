import { BlogSpaceContainer, BodyContainer, BlogBody, BlogTitle, SkillsContainer } from "../../styles/body"

export default function BodyDesktop({ matches }) {
  return (
    <BodyContainer>
      <BlogSpaceContainer>
        <BlogTitle>title</BlogTitle>
        <BlogBody>asdf</BlogBody>
      </BlogSpaceContainer>
      <SkillsContainer>
        
      </SkillsContainer>
    </BodyContainer>
  )
}