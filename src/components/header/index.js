import { HeaderContainer, HighlightSpanBox } from "../../styles/header"

export default function Header(){
  return (
    <HeaderContainer>
      <HighlightSpanBox>
        Holden's Website
      </HighlightSpanBox>
    </HeaderContainer>
  )
}